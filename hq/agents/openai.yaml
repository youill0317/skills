interface:
  display_name: "HQ"
  short_description: "사용자 대화와 병렬 서브에이전트 작업을 함께 지휘"
  default_prompt: "Use $hq to keep this conversation interactive while delegating substantive work to parallel subagents and integrating their evidence-backed results."
policy:
  allow_implicit_invocation: true
