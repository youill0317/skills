interface:
  display_name: "Research"
  short_description: "Run maximum-saturation evidence research into one Markdown record."
  default_prompt: "Use $research for maximum-saturation evidence research on: <topic>. Do not use quick/deep/lightweight modes; inspect sources, expand leads, counter-search, verify claims, and produce one Markdown record."
