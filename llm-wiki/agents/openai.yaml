interface:
  display_name: "LLM Wiki"
  short_description: "Compile sources into a grounded, evolving Obsidian wiki"
  default_prompt: "Use $llm-wiki to process my vault's _ingest queue into the appropriate source-grounded wiki."
